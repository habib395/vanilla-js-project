1) Difference between null and undefined:

undefined → variable is declared but not assigned.

null → explicitly assigned but “no value”.

2) map() vs forEach():

map() → creates and returns a new array with results.

forEach() → does not return anything.

3) Difference between == and ===:

== → compares values after type coercion.

=== → compares values and types .

4) Significance of async/await in API calls:

Makes asynchronous code look synchronous.

Easier to read, handle, and catch errors with friendly.

5) Scope in JavaScript:

Global Scope → accessible anywhere.

Function Scope → accessible inside the function.

Block Scope  → accessible only inside {} block,
